import { PendingScreen } from '@/components/system/PendingScreen'

/** SCR-302 시세 관리 — 컷 1b에서 선다. P4의 첫 화면이다(계획 §3). */

export default function PricesPage() {
  return <PendingScreen screen="SCR-302" title="시세 관리" cut="컷 1b" />
}
