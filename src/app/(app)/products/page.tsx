import { PendingScreen } from '@/components/system/PendingScreen'

/** SCR-201 ELS 목록 — 컷 2에서 선다. */

export default function ProductsPage() {
  return <PendingScreen screen="SCR-201" title="ELS 목록" cut="컷 2" />
}
