# 계산 로직 명세

**언제들어오나** — ELS 통합관리 시스템

| 항목 | 내용 |
|---|---|
| 문서 ID | DOC-007 |
| 버전 | 1.0 |
| 작성일 | 2026-07-26 |
| 작성자 | 민서 |
| 선행 문서 | DOC-001 프로젝트 개요서 v0.6, DOC-002 데이터 모델 v0.2 |
| 상태 | 검토 중 |

### 변경 이력

| 버전 | 일자 | 작성자 | 변경 내용 |
|---|---|---|---|
| 0.1 | 2026-07-26 | 민서 | 최초 작성 |
| 1.0 | 2026-08-01 | 민서 | **P6 컷 6 — §4.5 신설(예상 원천징수·세후 예상 수령액). 본 버전은 선행 명세다. §10의 TC-01~22와 §3의 판정 규칙은 무변경이다**(절대 규칙 #1). **(1) 산식이 새로 생긴 것이 아니라 «흩어져 있던 것을 정본화»했다** — `taxableIncome × separateTaxationRate`는 §5.5의 `withheld(F)`와 **같은 식**이고 입력만 다르다(연·개인 합계 vs 한 차수). 구현에서도 그 곱셈이 세 자리에 인라인되어 있었으므로(비교과세의 원천징수, 상환 실적의 §5.4 산출, 그리고 본 절) 이름 있는 순수 함수 하나로 모은다. **모으는 것은 리팩터링이지 규칙의 변경이 아니다** — 식·상수·절사가 전부 같으므로 §5.5의 값이 달라지지 않고 TC-01~22가 그 사실의 회귀망이다. DOC-010 AQ-67. **(2) ★ 「세후」가 붙는 값이 둘이 되었으므로 갈라 적었다.** `expectedNet`(차수·분리과세 고정·**참고값**)과 §7.4의 `netProceeds`(소유자·연도·최종 부담률·결론)는 단위도 세율도 성질도 다르며, **한 소유자의 한 해 `expectedNet`을 전부 더해도 `netProceeds`가 되지 않는다**(종합과세 구간에서 갈리고 그 갈림이 SCR-401의 존재 이유다). 그래서 이름을 「세후 회수」로 재사용하지 않고 DOC-005 §4에 별 용어로 등재했으며 §8.9에 혼동 주의를 두었다. **(3) ★ 표시 정합성을 위해 세 값을 «한 반올림값»에서 파생시킨다.** 각각 반올림하면 화면에서 `세후 + 원천징수 ≠ 세전`이 되고 **사용자가 뺄셈으로 그것을 본다** — §5.3이 지방소득세를 잔차로 두는 것과 같은 판단이다. 그 대신 §4.6의 과세 집계는 **반올림하지 않은** 값을 유지한다(반올림하면 20,000,000 경계가 움직인다). 두 경로가 ≤1원 어긋나는 조건과 어느 쪽이 이기는지의 근거를 본문에 적었다 — **행 하나의 자기 정합성이 이기는 이유는 사용자가 화면에서 그 뺄셈을 «할 수 있고» §4.6의 중간값은 «볼 수 없다»는 것이다.** **(4) `TAX_FREE` 분기를 명시했다** — §4.3에 의해 곱셈이 자동으로 0이 되지만, 「곱해서 0」과 「부과 대상이 아님」은 화면이 다르게 말해야 하는 상태다. **(5) `EARLY` 가정의 한계를 적었다** — 리자드가 붙은 차수는 실제 상환 시 이보다 «작다». 리자드 변형을 계산하지 않는 것은 §4.1의 「호출부가 명시한다」를 따르는 것이고, 대신 화면이 그 차수에 표식을 낸다. **(6) ★ 검산을 TC로 «만들지 않았다».** 실보유 상품의 증권사 대조표와 원 단위까지 일치하지만, 「TC-01~22」가 CLAUDE.md와 저장소 여러 곳에 문자열로 박혀 있어 스물셋째를 더하면 그 인용이 전부 낡는다 — 검증은 `tests/tax/withholding.test.ts`가 어차피 하므로 §4.5 본문의 **검산 블록**으로 둔다. 「무엇을 위해 번호를 늘리는가」에 답이 없으면 늘리지 않는다 |
| 0.9 | 2026-07-31 | 민서 | **P6 컷 1 — §11 RD-03의 기산 규약만 닫는다. §10의 TC-01~22과 §3의 판정 규칙은 무변경이다**(절대 규칙 #1). **(1) ★ 평가일 생성 산식이 `발행일 + n × 평가주기 − 1일`이 됐다.** 실보유 ELS 11건의 증권사 통지서를 대조했고 **65차수 전부에서 일정하게 −1일**이었다. ★ **그 균일성이 판별의 근거다** — 영업일 조정이면 휴일에 걸린 **일부 차수만** 움직이고 방향도 통상 **+**다. 전 차수가 균일하게 −1이면 조정이 아니라 **기산 규약**(「n개월 후의 전일」)이고, 그래서 휴장일 자료 없이 산식으로 닫을 수 있었다. 상수는 `lib/domain`에 두고 호출부가 **필수 인자로 명시한다**(기본값을 주면 새 호출부가 조용히 규약을 안 따른다 — 컴파일러가 잡게 한다). **(2) ★ 월 이동 → 클램프 → 일 이동 순서를 정본으로 못박았다.** 월말에서 갈리기 때문이다(`2026-08-31` + 6개월: 클램프 먼저면 `2027-02-27`, 일 이동 먼저면 `2027-02-28`). 근거 둘 — 「n개월 후의 전일」의 자연스러운 읽기이고, **클램프가 먼저인 것이 `addMonths`의 기존 성질을 보존한다**(항상 발행일 기준 가산으로 클램프 전파를 막는 것. 일 이동을 먼저 하면 기준 자체가 바뀐다). 대조표는 DOC-002 §4.8. **(3) ★★ 이 항목이 지목한 §5.2 충돌이 해소됐고, 그래서 RD-03의 두 하위 결정 중 하나만 필요해졌다.** 종전 비고는 「① 조정 경로를 화면에 두는가 ② 둔다면 전체 교체와 충돌하므로 예외를 어떻게 두는가」였다. **규약이 전역이므로 재생성이 멱등이고**(저장값이 이미 새 산식의 결과다) **화면에 칸을 두지 않아도 조정이 성립한다** — 즉 ①이 「두지 않는다」로 답되면서 ②가 발생하지 않는다. **(4) ★ 그럼에도 이 항목은 닫히지 않는다 — 잔여 둘을 명시했다.** ⓐ 휴장일 자료에 의한 **자동** 영업일 조정은 여전히 수단이 없고 선행은 Q-03이 아니라 **휴장일 자료원**이다 ⓑ **offset이 전역 규약이므로 상품별 규약 차이를 표현하지 못한다** — 다른 규약의 상품이 오면 그 상품의 전 차수가 하루씩 틀리고 **화면에 그 사실을 말할 자리가 없다.** 되살릴 조건이 그것이며, 그때 필요한 차수별 칸·상품별 필드가 **(3)에서 해소된 §5.2 충돌을 되돌린다.** 「부분 해결」로 뭉개지 않고 **닫힌 것과 남은 것을 갈라 적는다** |
| 0.8 | 2026-07-29 | 민서 | **P4b 컷 4 — §7.5 신설(다년도 누적·잔여 원금)과 RD-02 해결. 본 버전은 선행 명세다 — 구현은 컷 5 이후다. §10의 TC-01~22와 §3의 판정 규칙은 무변경이다**(절대 규칙 #1). (1) **§7.5 신설** — `redeemedBy` · `remainingPrincipal` · `cumulativeNet` · `cumulativeAssets`. DOC-011 §4.7의 두 값이 **어느 문서에도 산식이 없어** P3a부터 미구현으로 남아 있던 그 자리다. **§7의 제목은 그대로 둔다** — 이 장의 조직 원리는 「유량」이 아니라 집계 키 `(소유자, 귀속연도)`이며(§7.1이 첫 절인 이유) 연도 말 저량도 같은 키로 매겨진다. 제목을 바꾸면 산문 참조 넷이 뒤처진다. (2) **측정 시점을 「귀속연도의 말」로 확정하고 검산을 남겼다** — 기준일이 아니다. 그 둘은 **이중 계상 스위치**다: 기준일에서 재면 `asOf` 이후 같은 해에 상환되는 상품이 유량과 저량 양쪽에 잡혀 **정확히 그 상품의 원금만큼 과대**해진다(예시에서 208,460,000 대 308,460,000). 검산 숫자는 **`lib/tax`의 함수로 실측했다** — `F = 30,000,000`·`A = 0`에서 방식① 4,200,000 > 방식② 3,400,000이라 총세액 4,620,000이 원천징수액과 같고 추가납부가 **0**이며(§5.5가 규정한 정상 경우, 분기점 77,600,000) 최종 부담률 15.40%다. 두 경로의 검산이 일치한다(233,840,000). (3) **배타적·전체적 분할의 근거를 표로 적었다** — `gross`가 원금 반환분을 포함하므로 한 상품이 두 항 중 정확히 하나에만 들어간다. **그 표의 둘째 줄이 이름의 근거다**: `Y₀` 이전에 상환된 상품은 어느 항에도 없으므로 이 값은 「보유 자산의 총액」이 아니라 전망 구간 안의 누적이며, DOC-011 §4.7의 `totalAssets`는 **산식이 옳고 이름이 틀렸다**(DOC-005 §8.8에 혼동 주의를 등재했다). (4) **적용 차수가 없는 셋을 배제하지 않는다** — `redeemedBy`를 귀속연도가 아니라 **상환 레코드 존재**로 정의해 특수 처리 없이 흡수한다. 배제하면 그 원금이 화면에서 조용히 증발하며, 여섯 행 전부의 잔여 원금에 남는 것이 **E-07의 시각적 형태**다(화면이 그 사실을 말해야 한다). (5) **추정 표식을 두 축으로 갈랐다** — 행 표식과 화면 전체 고지. 하나로 두면 미상환 상품이 하나라도 있을 때 모든 행이 참이 되어 행 표식의 정보량이 0이 된다. (6) **건강보험료는 누적에서 차감하지 않는다**(§7.4에 그 사실을 명시). 근거는 추정도가 다르다는 것이다 — 건보료는 세대 단위 부과인데 본 시스템은 세대를 모델링하지 않는다(A-06). 표의 별 열로 보여 준다. (7) **§7.2 — RD-02를 해결로 전환.** 기본값 「다음 도래 평가일」을 **확정 유지**하고 사용자 조정을 `getForecast` 전용 **비영속 `scenario`**로 둔다(스키마 변경 0). `scenario`의 규칙(선택 범위·경과 차수·없는 차수)은 **P4c에 배정**했다 — 기본값만으로 §7.5가 완결되므로 분리할 수 있다. §7.2에 「정할 수 없는 세 경우」도 함께 적었다. (8) **§8 파이프라인에 10단계** 추가 — 9단계까지와 다른 유일한 점이 측정 시점이라는 것을 적었다 |
| 0.7 | 2026-07-29 | 민서 | **P4.5 — §11 RD-03의 비고가 실재하지 않는 폴백을 전제했다(U-01). 계산 규칙은 변경하지 않았다.** 그 비고는 「휴장일 처리. **현재는 사용자 수정에 의존**」이었는데 **평가일에는 화면에 입력 칸이 없다** — SCR-204가 산식으로 생성하고 저장마다 재생성한다(DOC-008 §5 · DOC-002 §4.8). 즉 이 미결에는 폴백이 없고 **조정이 필요한 상품이 오면 지금은 방법이 없다.** 닫을 때 함께 정할 것 둘을 비고에 적었다(조정 경로를 화면에 두는가 / 둔다면 DOC-011 §5.2의 전체 교체와 충돌하므로 그 예외를 어떻게 두는가). **§10의 TC-01~22와 §3의 판정 규칙은 무변경이다** — 절대 규칙 #1에 따라 정본을 건드리지 않았다. 관련하여 SCR-203의 상환 유형 기본값 결함(마지막 차수에서 `EARLY`가 채워졌다)은 **본 문서가 아니라 화면에서** 고쳤다: `evaluateCondition`에 만기 개념을 넣는 것이 정본 변경이므로, 판정 축(3값)과 실적 축(4값)의 차이를 화면이 흡수하도록 DOC-008 §5에 규정을 두었다 **★ 이 버전은 적대적 반증을 지났다** — 5축 반증이 이 커밋의 주장에서 결함을 냈고 전부 정정했다(BLOCKER 2종: 「실측 1,028개」가 계산값 1,027이고 그 주소는 `<option>`을 0개 렌더한다 · 음성 대조 표 두 줄의 건수. WRONG: V-19 인용이 거짓이었다(§6의 V-19는 문자열 길이 규칙이며 `/^\d+$/`는 구현이 ID 재사용 규약으로 빌린 것 — **내가 닫으려던 부류를 내가 만들었다**) · 「추가납부가 양수」가 0이다 · AQ-36의 「여덟 칸 중 여섯」이 다섯이다 · `worstOf`를 담는 뷰가 셋이 아니라 넷이다 · 문서 접두사 없는 § 참조 다섯 · 항진명제 케이스 둘 · 퇴화 픽스처 하나 · DOC-000 버전 표 드리프트 다섯 행). |
| 0.6 | 2026-07-28 | 민서 | P4 컷 1a 선행. **§3.4 표시 등급 표에 코드 값 열을 추가하고 표시 문자열의 정본을 DOC-005 §6.1로 지정했다.** 종전에는 같은 등급이 본 문서(「KI 이하 (확인 필요)」)와 DOC-008 §5(「KI 배리어 하회 — 확인 필요」)에 다르게 적혀 있어 화면이 어느 쪽을 옮겨 적어도 문서와 어긋났고, 그 갈림을 볼 테스트가 없었다. 판정 알고리즘은 바뀌지 않는다 |
| 0.5 | 2026-07-27 | 민서 | P3a 조회 계약 착수에 따른 정정. **§3.1 "최신가"의 정의에 기준일 상한을 추가** — 종전 정의("`asset_prices`의 최신 `as_of_date` 값")에는 상한이 없어, 기준일보다 미래인 시세 행이 존재하면 **미래의 가격으로 현재의 조건을 판정**한다. DOC-011 V-17이 미래 일자 시세를 거부하나 그 규칙은 계약 계층에만 있고 DB `CHECK`로 만들 수 없다고 같은 문서가 명시했으므로(`current_date`는 IMMUTABLE이 아니다), 마이그레이션·수동 SQL로 도달 가능한 상태다. 판정의 다른 모든 입력이 기준일에 종속되는데 시세만 벽시계를 따르면 스냅숏이 내부적으로 어긋난다 |
| 0.2 | 2026-07-27 | 민서 | P1 구현 착수에 따른 정정. (1) §5·§6 본문의 세율·기준금액 리터럴을 주입 상수 참조로 교체 — 1장이 "상수값을 정의하지 않는다"고 선언했으나 본문에 `0.14`·`0.154`·`20,000,000`·`10,000,000`이 직접 기재되어 자기모순이었음. §2에 주입 상수 목록 신설. (2) **분리과세율(15.4%)과 분리과세 소득세율(14%)을 별개 상수로 분리** — §5.2 비교과세는 소득세분만, §5.5 원천징수는 지방세 포함 총액을 사용하므로 하나의 상수로 둘 수 없음. (3) §6.1의 직장가입자·피부양자 문턱을 종합과세 기준금액과 별개 상수로 분리 — 2026년 기준 금액이 같으나 의미가 다르고 법령이 함께 개정된다는 보장이 없음. (4) §11 RD-01 구현 방침 명기 |
| 0.4 | 2026-07-27 | 민서 | P2 산출물 대조 검증(P2.5)에 따른 정정. (1) **§3.1에 기초자산 0건의 처리 신설** — 종전에는 `worstOf([])`가 E-01과 같은 `null`을 반환하여, 시세가 오면 해소되는 일시적 상태와 DOC-002 I-07을 위반한 구조적 상태가 구분되지 않았다. 화면은 두 경우를 "시세 없음"으로 같게 표시하며 오지 않을 시세를 기다린다. 0건은 거부한다. (2) **§3.4에 `ki_barrier`·`ki_observation`·`ki_touched_at`의 정의역 조건 명기** — E-06이 노낙인 상품의 KI 판정을 생략하므로 `ki_barrier`가 없는데 `ki_touched_at`이 있는 행은 그 기록이 판정에서 무시되어 리자드의 KI 미터치 요구가 충족으로 판정되고 수령액이 틀린다. 판정 알고리즘은 그대로 두고 DOC-002 I-11·I-15가 입력의 정의역을 좁힌다 |
| 0.3 | 2026-07-27 | 민서 | P1 구현 대조에서 발견된 갭 반영(P1.5). (1) **§9에 E-05·E-07의 반환 계약 신설**(§9.1·§9.2) — 표의 "처리" 문구만으로는 구현할 수 없는 항목이었고, 실제로 E-05는 구현·테스트가 전무했으며 E-07은 "모든 평가일 경과" 한 경우만 처리되어 중간 차수의 경과를 놓쳤음. (2) **§7.1 집계를 평탄한 합산에서 (소유자, 귀속연도) 키별 분할로 재정의** — 종전 서술은 "호출부가 이미 필터링했다"를 전제하여 절대 규칙 #7이 코드 어디에서도 강제되지 않았음. §7.3의 `F` 정의도 키 단위로 정정. (3) **§4.1에 적용 쿠폰율 선택 규칙 명시** — "리자드 상환을 가정하는 경우 `r` 대신 `lizard_coupon_rate`를 사용한다"가 서술로만 있어 선택 주체와 `lizard_coupon_rate IS NULL`의 처리가 정의되지 않았음. (4) §8 파이프라인 3·7단계에 적용 조건 명기. (5) §11 RD-06 신설 |

---

## 1. 목적 및 적용 범위

본 문서는 시스템이 수행하는 모든 도메인 계산의 알고리즘을 정의한다. 조건 판정, 수령액 산출, 과세 금융소득 산정, 금융소득종합과세, 건강보험료, 연도별 집계를 다룬다.

**본 문서는 알고리즘을 정의하며 상수값을 정의하지 않는다.** 세율·요율 등 법령 기반 상수는 `tax_constants` / `tax_brackets`에 연도별로 저장된다(DOC-002 M-05). 본문에 등장하는 구체적 수치는 2026년 기준 예시이며, 구현 시 상수를 하드코딩해서는 안 된다.

10장의 검증 케이스는 **회귀 테스트로 고정한다**(K-08). 계산 로직 변경 시 해당 케이스가 모두 통과해야 한다.

> **면책:** 본 시스템의 세액 산출은 추정·계획 목적이며 세무 신고 자료가 아니다(DOC-001 C-04).

---

## 2. 표기 규약

| 기호 | 의미 |
|---|---|
| `P` | 투자원금 (principal) |
| `r` | 연쿠폰율 (annual_coupon_rate) |
| `m` | 평가주기 개월 수 |
| `n` | 차수 (round_no) |
| `B(n)` | n차 조기상환 배리어 |
| `L(n)` | n차 리자드 배리어 |
| `W` | 워스트오브 (worst-of ratio) |
| `F` | 특정 소유자·연도의 과세 금융소득 총액 |
| `A` | 금융소득 외 종합소득 과세표준 |
| `T(x)` | 종합소득세 누진 산출세액 함수 (5.1) |

**단위 규약**

- 비율은 소수로 정규화하여 저장·계산한다 (90% → `0.9`). DOC-002 M-04.
- 금액은 원 단위 정수로 취급한다. 중간 계산은 고정소수점으로 수행하고 최종 표시 시 반올림한다.
- 모든 금액 계산에서 부동소수점 연산을 금지한다. DOC-002 M-03.

**주입 상수**

본문의 알고리즘은 아래 상수를 **인자로 주입받는다**(DOC-010 DEP-03). 값은 `tax_constants`·`tax_brackets`에서 연도별로 조회하며, 계산 모듈이 직접 조회하거나 하드코딩하지 않는다. 2026년 열의 값은 검증 케이스(10장)의 기준이며 명세의 일부가 아니다.

| 기호 | `tax_constants.key` | 표준 용어 | 2026년 |
|---|---|---|---|
| `separateTaxationRate` | `separate_taxation_rate` | 분리과세율 | 0.154 |
| `separateTaxationIncomeTaxRate` | `separate_taxation_income_tax_rate` | 분리과세 소득세율 | 0.14 |
| `comprehensiveTaxationThreshold` | `comprehensive_taxation_threshold` | 종합과세 기준금액 | 20,000,000 |
| `localIncomeTaxRate` | `local_income_tax_rate` | 지방소득세 부가율 | 0.1 |
| `healthInsuranceRate` | `health_insurance_rate` | 건강보험료율 | 0.0719 |
| `longTermCareRate` | `long_term_care_rate` | 장기요양보험 비율 | 0.1314 |
| `regionalIncomeThreshold` | `regional_income_threshold` | 지역가입자 금융소득 반영 기준 | 10,000,000 |
| `employeeNonWageThreshold` | `employee_non_wage_income_threshold` | 직장가입자 보수외소득 기준 | 20,000,000 |
| `dependentIncomeThreshold` | `dependent_income_threshold` | 피부양자 소득 기준 | 20,000,000 |

누진세율 구간은 `tax_brackets`에서 `(lowerBound, rate, progressiveDeduction)` 목록으로 주입받는다(5.1).

> **분리과세율과 분리과세 소득세율을 분리하는 이유.** 15.4%는 소득세 14%와 지방소득세 1.4%의 합이다. 비교과세(5.2)의 방식①·방식②는 **소득세만** 산출한 뒤 5.3에서 지방소득세를 별도로 가산하므로 소득세분 14%를 쓴다. 반면 원천징수액(5.5)은 이미 지방세를 포함한 실제 징수액이므로 15.4%를 쓴다. 두 값을 하나의 상수로 두면 어느 한쪽이 반드시 틀린다.

> **직장가입자·피부양자 문턱을 종합과세 기준금액과 분리하는 이유.** 2026년 기준 세 값이 모두 2,000만원이나, 앞의 둘은 국민건강보험법상 부과 문턱이고 뒤는 소득세법상 종합과세 기준금액이다. 근거 법령이 달라 함께 개정된다는 보장이 없으므로 별도 상수로 관리한다.

**반올림 규칙**

| 대상 | 규칙 |
|---|---|
| 중간 계산값 | 반올림하지 않음 (전 구간 고정소수점 유지) |
| 화면 표시 금액 | 원 단위 반올림 |
| 세액·보험료 최종값 | 원 단위 절사 (RD-01 참조) |

---

## 3. 조건 판정

### 3.1 워스트오브 (Worst-of)

기초자산 중 기준가 대비 등락률이 가장 낮은 자산의 비율.

```
W = min( currentPrice(a) / basePrice(a) )   for a in underlyings(els)
```

**전제**

- `currentPrice`는 **기준일(`asOf`) 이하의 `as_of_date` 중 가장 큰 행**의 값이다. 상한이 없으면 미래 일자 시세로 현재의 조건을 판정한다 — DOC-011 V-17이 그 입력을 거부하나 계약 계층 한 층뿐이므로(DB `CHECK`로 표현 불가) 판정 쪽에서도 상한을 둔다. 판정의 다른 입력(평가일 경과·D-Day·귀속연도)이 모두 기준일에 종속되므로 시세만 벽시계를 따르면 한 화면에 두 시점이 섞인다.
- 기준가와 현재가는 동일 통화이므로 비율 계산에서 통화가 상쇄된다. 환율 변환은 수행하지 않는다(DOC-002 D-03).
- 어느 하나의 기초자산이라도 현재가가 없으면 `W`는 산출 불가(`null`)이며, 조건 판정 결과도 `null`로 처리한다. 임의의 기본값을 대입해서는 안 된다.
- **기초자산이 0건이면 산출 불가가 아니라 전제조건 위반이다.** `null`을 반환하지 않고 **거부한다.**

> **0건과 E-01의 `null`은 성질이 다르다.** E-01의 `null`은 시세가 수집되면 값이 생기는 **일시적** 상태다. 기초자산 0건은 DOC-002 I-07이 금지한 **구조적** 위반이며 시세를 기다려도 해소되지 않는다. 둘을 같은 `null`로 반환하면 화면이 "시세 없음"을 표시하면서 영원히 오지 않을 시세를 기다리게 된다. §9.1이 E-01의 `null`(판정 보류)과 E-05의 `null`(판정 대상 아님)을 구분한 것과 같은 이유다.
>
> 거부는 §9의 예외 처리 대상이 아니다 — E-표는 **발생할 수 있는 상황**의 처리 규칙이고, 0건은 발생해서는 안 되는 입력이다. `tax_brackets`가 빈 목록으로 주입되는 경우를 시드 오류로 보고 거부하는 것(§5.1)과 같은 부류다.

### 3.2 조기상환 조건

n차 평가일에 대하여:

```
조기상환 충족  ⟺  W ≥ B(n)
```

### 3.3 리자드 조건

n차에 리자드가 정의된 경우(`lizard_barrier IS NOT NULL`)에만 평가하며, **조기상환 조건이 충족되지 않았을 때에만** 판정한다.

```
리자드 충족  ⟺  W < B(n)
              ∧  W ≥ L(n)
              ∧  ( lizard_requires_no_ki = false  ∨  KI 미터치 )
```

판정 우선순위는 다음과 같다.

```
1. W ≥ B(n)          → 조기상환 (EARLY)
2. 리자드 충족        → 리자드상환 (LIZARD)
3. 그 외              → 다음 차수로 이월
```

### 3.4 KI 상태

```
KI 미설정   ⟺  ki_barrier IS NULL          (노낙인 상품)
KI 터치됨   ⟺  ki_touched_at IS NOT NULL
KI 미터치   ⟺  위 두 경우가 아님
```

**보조 판정.** 수집된 시세로 `W < ki_barrier`가 관측되면 터치 후보로 표시하고 사용자 확인을 요청한다. 시스템이 자동으로 `ki_touched_at`을 확정하지 않는다(DOC-002 D-04). 관측 방식(`ki_observation`)이 `CONTINUOUS`인지 `CLOSING`인지에 따라 요구되는 데이터가 다르므로, `ki_barrier`가 있으면 관측 방식도 반드시 있어야 한다(DOC-002 I-11).

> **`ki_barrier IS NULL`이면 `ki_touched_at`을 보지 않는다.** 위 표의 첫 줄부터 `ki_barrier`가 있는 상품을 전제하며, E-06이 노낙인 상품의 KI 판정을 생략하고 리자드의 KI 요구 조건을 항상 충족으로 처리한다. 따라서 `ki_barrier`가 없는데 `ki_touched_at`이 기록된 행은 **그 기록이 판정에서 완전히 무시되고**, 리자드 상환이 잘못 판정되어 수령액이 어긋난다. 이 명세는 그런 행의 존재를 전제하지 않으므로 DOC-002 I-15가 저장 시점에 막는다 — 판정 알고리즘을 바꾸는 것이 아니라 입력의 정의역을 좁히는 것이다.

**표시 등급 (참고)**

| 조건 | 상태 | 코드 값 |
|---|---|---|
| `ki_touched_at IS NOT NULL` | KI 터치 | `TOUCHED` |
| `W ≤ ki_barrier` | KI 이하 (확인 필요) | `BELOW` |
| `W ≤ ki_barrier × 1.1` | 주의 | `WARNING` |
| 그 외 | 안전 | `SAFE` |

> **위 「상태」 열은 조건과 등급의 대응을 보이기 위한 참고 표기이고, 화면에 나가는
> 문자열의 정본은 DOC-005 §6.1이다 (v0.6).** 그 표는 `BELOW`를 「KI 배리어 하회」로
> 적는다 — DOC-005 §9가 「KI 배리어」를 표준 표기로 정했고 「확인 필요」는 등급이
> 아니라 **조치**를 말하는 표현이어서 조치 목록(`attentionReason.KI_BELOW`)에만
> 붙기 때문이다. 노낙인 상품은 이 표의 정의역 밖이며 코드 값이 `NO_KI`다.

---

## 4. 수령액 및 과세 금융소득

### 4.1 예상 수령액 (미상환 상품)

n차에 상환된다고 가정할 때의 세전 수령액.

```
grossExpected(n) = P × ( 1 + r × (m × n) / 12 )
```

**적용 쿠폰율.** 위 식의 `r`은 상수가 아니라 판정 결과(§3.3)에 따라 선택된 값이다.

```
applicableCouponRate(condition, r, lizardCouponRate)
  = r                     if condition = EARLY
  = lizardCouponRate      if condition = LIZARD
```

- `CARRY_OVER`는 해당 차수에 상환되지 않으므로 적용 쿠폰율이 정의되지 않는다. 호출 대상이 아니다.
- 아직 평가일이 도래하지 않은 차수는 판정값이 없으므로 **`EARLY`(정상 조기상환)를 가정한다.** 예상 수령액은 추정값이며, 리자드 상환을 가정하려면 호출부가 `LIZARD`를 명시한다.
- `condition = LIZARD`이고 해당 차수의 `lizard_coupon_rate`가 `NULL`이면 **오류로 처리한다.** 원금만 상환되는 리자드 변형은 `0`으로 표기하며 `NULL`로 표기하지 않는다(Q-04). `NULL`을 `0`이나 `r`로 임의 대체하면 수령액이 실제와 어긋난다 — 전자는 과소, 후자는 과대 추정이다.

### 4.2 확정 수령액 (상환 완료 상품)

`redemptions.gross_amount`를 그대로 사용한다. 시스템 추정값으로 대체하지 않는다(DOC-002 A-04).

### 4.3 과세 금융소득

```
if account_type = TAX_FREE:
    taxableIncome = 0                      # 비과세 계좌는 종합과세 대상 제외

elif 상환 완료:
    taxableIncome = redemptions.taxable_income      # 증권사 확정값

else:  # 미상환 (추정)
    taxableIncome = max(0, grossExpected(n) − P)
```

### 4.4 손실 상환의 취급

**ELS 손실은 다른 금융소득과 통산되지 않는다.** 따라서 손실 상환(`MATURITY_LOSS`)의 과세 금융소득은 음수가 아니라 `0`이다.

```
포트폴리오 손익  =  gross_amount − P        (음수 가능)
과세 금융소득    =  0                        (음수 불가)
```

예: A 상품에서 1,000만원 손실, B 상품에서 2,000만원 이익이 발생한 경우

| 항목 | 값 |
|---|---|
| 포트폴리오 순손익 | +1,000만원 |
| 과세 금융소득 | **2,000만원** (1,000만원이 아님) |

이 분기는 사용자가 오해하기 쉬우므로 UI에서 두 값을 별도 항목으로 표시한다.

### 4.5 예상 원천징수와 세후 예상 수령액 (참고용 · 분리과세 기준)

미상환 차수의 **참고용** 세후 금액이다. 그 차수에 분리과세로 종결된다고 가정하며 비교과세(§5.2)를 반영하지 않는다.

```
expectedWithholding(n) = truncate( taxableIncome(n) × k.separateTaxationRate )
expectedNet(n)         = grossExpected(n) − expectedWithholding(n)
expectedPnl(n)         = grossExpected(n) − P                        (음수 가능)
```

- `taxableIncome(n)`은 §4.3의 **미상환 분기**다 — `max(0, grossExpected(n) − P)`. 상환 완료 상품의 확정값(`redemptions.taxable_income`)을 쓰지 않는다. 이 축 전체가 「예상」이므로 확정값을 섞으면 한 열에 추정과 확정이 공존한다.
- 비율은 §2의 주입 상수 `separateTaxationRate`다. **`separateTaxationIncomeTaxRate`(14%)가 아니다** — 원천징수액은 지방소득세를 포함한 실제 징수액이며 그 구분은 §2 각주가 정본이다. 잘못 고르면 10% 과소 징수가 그럴싸한 숫자로 지나간다.
- 절사 단위는 §2의 반올림 규칙(세액 최종값 = 원 단위 절사)을 따르고 RD-01에 따라 **주입받는다.**

**비과세 계좌는 원천징수가 없다.**

```
if account_type = TAX_FREE:
    expectedWithholding(n) = 0
    expectedNet(n)         = grossExpected(n)
```

§4.3이 비과세 계좌의 과세 금융소득을 `0`으로 정하므로 위 곱셈이 자동으로 0이 되지만 **분기를 명시한다.** 「곱해서 0」과 「부과 대상이 아님」은 화면이 다르게 말해야 하는 상태이고, 계약도 `accountType`을 함께 실어 그 구분을 나른다(DOC-011 §4.4).

**이 값은 §7.4의 세후 회수와 «다른 값»이다.** 한 표에 나란히 두면 안 된다.

| | `expectedNet` (본 절) | `netProceeds` (§7.4) |
|---|---|---|
| 단위 | **차수** (한 상품의 한 차수) | **(소유자, 귀속연도)** |
| 세율 | 분리과세율 하나 (`k.separateTaxationRate`) | 비교과세 최종 부담률 (§5.2·§5.4) |
| 입력 | 그 상품의 계약 조건뿐 | 그 해 전체 금융소득 `F`와 다른 종합소득 `A` |
| 성질 | **참고값.** 종합과세 대상이면 실제 부담은 이보다 크다 | 그 해의 추정 결론 |

**그러므로 「세후 회수」라고 부르지 않는다** — DOC-005 §4가 그 이름을 연도 단위 값에 이미 배정했다(절대 규칙 #9). 표준 용어는 **「세후 예상 수령액」**이며, 한 소유자의 한 해 `expectedNet`을 전부 더해도 `netProceeds`가 되지 않는다(DOC-005 §8.9).

**표시 정합성 — 세 값이 한 반올림값에서 파생된다.** `grossExpected(n)`을 원 단위로 한 번 반올림한 뒤 그 값에서 나머지를 유도한다. 각각 따로 반올림하면 화면에서 `expectedNet + expectedWithholding ≠ grossExpected`가 되고 **사용자가 뺄셈으로 그것을 본다.** §5.3이 총세액을 1차 값으로 두고 지방소득세를 잔차로 구하는 것과 같은 판단이다.

그 대신 **§4.6의 과세 집계는 반올림하지 않은 값을 계속 쓴다.** `P × r × (m·n)/12`가 소수인 경우(`m ∈ {1,2,5}`에서 `m·n/12`가 유한소수가 아니다) 두 경로가 **≤1원** 어긋날 수 있는데, 집계 쪽을 반올림하면 종합과세 기준금액 20,000,000의 경계가 움직인다. 행 하나의 자기 정합성이 여기서 이기는 이유는 **사용자가 화면에서 그 뺄셈을 할 수 있고 §4.6의 중간값은 볼 수 없다**는 것이다.

**`EARLY` 가정의 한계.** `grossExpected(n)`이 §4.1의 `EARLY` 가정 위에 있으므로, 리자드 조건이 붙은 차수가 실제로 `LIZARD`로 상환되면 금액이 **이보다 작다**(`lizard_coupon_rate`가 적용된다). 화면은 그 차수에 리자드 표식을 함께 낸다.

**검산 — 검증 케이스가 «아니다».**

§10의 TC-01~22는 종합과세·건강보험료·조건 판정의 집합이며 이 산식은 그 집합에 넣지 않는다. 「TC-01~22」가 CLAUDE.md 절대 규칙 #1과 저장소 여러 곳에 문자열로 박혀 있어 스물셋째를 더하면 그 인용이 전부 낡는데, 이 산식의 검증은 `tests/tax/withholding.test.ts`가 어차피 한다.

| 항목 | 값 |
|---|---|
| 입력 | `P = 50,000,000` · `r = 0.1722` · `m = 6` · `n = 1` · `GENERAL` · `k.separateTaxationRate = 0.154` |
| 예상 수령액 | 54,305,000 |
| 과세 금융소득 | 4,305,000 |
| 예상 원천징수 | 662,970 |
| **세후 예상 수령액** | **53,642,030** |
| 예상 손익 | +4,305,000 |
| 같은 입력, `TAX_FREE` | 과세 금융소득 0 · 원천징수 0 · 세후 예상 수령액 **54,305,000** |

실보유 ELS의 증권사 대조표와 **원 단위까지 일치한다.** 기대값이 전부 정수이므로 RD-01의 절사 단위와 무관하다(TC-01~22가 가진 성질과 같다).

**§10의 TC-01~22와 §3의 판정 규칙은 무변경이다**(절대 규칙 #1).

---

## 5. 금융소득종합과세

### 5.1 누진 산출세액 함수

`tax_brackets`에서 과세표준 `x`가 속한 구간의 세율과 누진공제를 조회한다.

```
T(x) = 0                                          if x ≤ 0
     = x × rate(x) − progressiveDeduction(x)      otherwise
```

**2026년 기준 구간 (예시 — 실제 값은 `tax_brackets`에서 조회)**

| 과세표준 하한 | 세율 | 누진공제 |
|---|---|---|
| 0 | 6% | 0 |
| 1,400만 | 15% | 126만 |
| 5,000만 | 24% | 576만 |
| 8,800만 | 35% | 1,544만 |
| 1억 5,000만 | 38% | 1,994만 |
| 3억 | 40% | 2,594만 |
| 5억 | 42% | 3,594만 |
| 10억 | 45% | 6,594만 |

### 5.2 비교과세

금융소득이 종합과세 기준금액을 초과하는 경우, 두 방식으로 산출한 세액 중 **큰 값**을 적용한다. 이하 `k`는 2장의 주입 상수 집합을 가리킨다.

```
방식 ①(분리 기준) = F × k.separateTaxationIncomeTaxRate + T(A)
방식 ②(종합 기준) = k.comprehensiveTaxationThreshold × k.separateTaxationIncomeTaxRate
                  + T( max(0, F − k.comprehensiveTaxationThreshold) + A )

산출세액 = max( 방식①, 방식② )
```

두 방식은 **소득세만** 산출한다. 지방소득세는 5.3에서 가산하므로 여기서 15.4%가 아니라 소득세분 14%를 쓴다(2장 참조).

**금융소득 귀속세액**(국세)은 전체 산출세액에서 금융소득이 없었을 때의 세액을 차감하여 구한다.

```
def financialIncomeTax(F, A, k):
    if F ≤ 0:
        return 0
    if F ≤ k.comprehensiveTaxationThreshold:
        return F × k.separateTaxationIncomeTaxRate      # 분리과세로 종결

    method1 = F × k.separateTaxationIncomeTaxRate + T(A)
    method2 = k.comprehensiveTaxationThreshold × k.separateTaxationIncomeTaxRate
            + T( max(0, F − k.comprehensiveTaxationThreshold) + A )
    return max(method1, method2) − T(A)
```

**기준금액 이하의 조기 반환은 생략할 수 없다.** 비교과세식을 기준금액 이하 구간까지 확장하면 틀린 값이 나온다. `F = 10,000,000`, `A = 0`인 경우 방식① = 1,400,000, 방식② = 2,800,000이므로 `max(①,②) − T(A) = 2,800,000`이 되지만, 정답은 원천징수로 종결되는 1,400,000이다. 방식②는 기준금액까지를 항상 전액 과세한 것으로 계산하므로 기준금액 미달 구간에서 성립하지 않는다. 두 방식이 교차하는 구간은 `F > k.comprehensiveTaxationThreshold`뿐이다.

**방식①·방식②의 표시.** `F ≤ k.comprehensiveTaxationThreshold`인 경우 비교과세를 적용하지 않으므로 두 방식 값은 의미가 없다. 화면·계약 계층에는 값이 아니라 **미적용(null)** 으로 전달한다(DOC-011 §4.6).

**`A`의 취급.** `A`가 미입력(null)인 경우와 `0`인 경우의 결과는 동일해야 한다. 구현 시 `A = coalesce(A, 0)`으로 정규화한다.

### 5.3 지방소득세

지방소득세는 소득세액에 `localIncomeTaxRate`(2026년 10%)를 곱한 값이다.

```
localIncomeTax(F, A) = financialIncomeTax(F, A) × k.localIncomeTaxRate
totalTax(F, A)       = financialIncomeTax(F, A) × ( 1 + k.localIncomeTaxRate )
```

### 5.4 최종 부담률

```
effectiveRate(F, A) = totalTax(F, A) / F        (F > 0)
                    = 0                          (F ≤ 0)
```

### 5.5 추가납부 예상액

상환 시점에 원천징수가 이미 이루어졌으므로, 종합과세 확정 세액과의 차액만 추가 납부한다. 원천징수액은 지방소득세를 포함한 실제 징수액이므로 `separateTaxationRate`(2026년 15.4%)를 쓴다.

```
withheld(F)         = F × k.separateTaxationRate
additionalTax(F, A) = max( 0, totalTax(F, A) − withheld(F) )
```

**중요 — 추가납부 0이 정상인 경우.** `A`가 작을 때 방식①이 방식②보다 크면 실효세율이 원천징수율로 유지되어 추가납부가 발생하지 않는다. 이는 오류가 아니다.

다른 종합소득이 없는 경우(`A = 0`) 추가납부가 처음 발생하는 지점은 **F = 77,600,000원**이다. 2026년 상수로 유도하면 다음과 같다.

```
방식① = 방식②
F × 0.14 = 20,000,000 × 0.14 + (F − 20,000,000) × 0.24 − 5,760,000
0.14F − 2,800,000 = 0.24F − 10,560,000
0.10F = 7,760,000
F = 77,600,000
```

이 임계값은 `A`에 따라 이동하므로 하드코딩하지 않는다. 위 값은 검증 케이스(10장)의 기준점으로만 사용한다.

### 5.6 구간별 실질 한계 부담률

금융소득이 1원 증가할 때의 추가 부담률. 세금과 건강보험을 합산한다.

```
marginalRate(bracket) = bracketRate × ( 1 + k.localIncomeTaxRate )   # 지방소득세 포함
                      + k.healthInsuranceRate                        # 건강보험료율
                      + k.healthInsuranceRate × k.longTermCareRate   # 장기요양보험료
```

2026년 기준 산출 결과 (건강보험료율 7.19%, 장기요양 13.14%):

| 소득세율 | 세금 한계 | 건강보험 | 장기요양 | **실질 한계** |
|---|---|---|---|---|
| 6% | 6.60% | 7.19% | 0.94% | **14.73%** |
| 15% | 16.50% | 7.19% | 0.94% | **24.63%** |
| 24% | 26.40% | 7.19% | 0.94% | **34.53%** |
| 35% | 38.50% | 7.19% | 0.94% | **46.63%** |
| 38% | 41.80% | 7.19% | 0.94% | **49.93%** |
| 40% | 44.00% | 7.19% | 0.94% | **52.13%** |
| 42% | 46.20% | 7.19% | 0.94% | **54.33%** |
| 45% | 49.50% | 7.19% | 0.94% | **57.63%** |

직장가입자와 지역가입자의 **한계율은 동일**하다. 차이는 한계율이 아니라 **부과가 시작되는 문턱**에 있다(6.1 참조).

---

## 6. 건강보험료

### 6.1 가입유형별 산정 소득

```
def insuranceBase(F, type, k):
    if type = REGIONAL:      # 지역가입자
        return F if F > k.regionalIncomeThreshold else 0    # 초과 시 전액 반영
    if type = EMPLOYEE:      # 직장가입자
        return max(0, F − k.employeeNonWageThreshold)       # 보수외소득 초과분만
    if type = DEPENDENT:     # 피부양자
        return F if F > k.dependentIncomeThreshold else 0   # 초과 시 자격 상실 → 전액
    return 0                                                # NONE
```

문턱 구조가 유형별로 다르다는 점에 유의한다. 지역가입자는 `regionalIncomeThreshold`(2026년 1,000만원) 초과 시 **전액**이 반영되므로 문턱 부근에서 부담이 계단식으로 증가한다.

세 문턱은 2026년 기준으로 각각 1,000만·2,000만·2,000만원이나 **서로 다른 상수**다. 뒤의 두 값이 종합과세 기준금액과 같은 것도 우연이며, 근거 법령이 다르므로 하나의 상수로 공유하지 않는다(2장 참조).

### 6.2 보험료 산출

```
healthInsurance(F, type) = insuranceBase(F, type, k) × k.healthInsuranceRate
longTermCare(F, type)    = healthInsurance(F, type) × k.longTermCareRate
totalInsurance           = healthInsurance + longTermCare
```

장기요양보험료는 금융소득이 아니라 **건강보험료에** 비율을 곱한 값이다(DOC-005 §5).

### 6.3 산출값의 한계

지역가입자 보험료는 실제로는 주민등록세대 단위로 소득·재산을 합산하여 부과되나, 본 시스템은 세대 구성과 재산을 모델링하지 않는다. 따라서 산출값은 **개인의 금융소득만 반영한 추정치**이며 실제 부과액과 차이가 있다(DOC-001 A-06). 이 취지를 UI에 고지한다.

---

## 7. 연도별 집계

### 7.1 집계 단위

집계 키는 항상 **(소유자, 귀속연도)** 다. 사용자 간 합산은 어떤 경우에도 수행하지 않는다(DOC-002 D-02).

집계는 **평탄한 합산이 아니라 키별 분할**로 정의한다. 여러 소유자·연도가 섞인 항목 집합을 받으면 키별로 나누어 각각 집계하며, 서로 다른 키의 값이 하나의 합계에 들어가는 경로를 두지 않는다.

```
groupByOwnerYear(items) = [ (key, { item ∈ items | key(item) = key }) | key ∈ distinct(key(items)) ]
```

단일 키 집계는 키를 **명시적 인자로 받고**, 키와 다른 항목이 섞여 있으면 거부한다.

```
aggregateFinancialIncome(key, items) = ERROR         if ∃ item ∈ items : key(item) ≠ key
                                     = (key, F)      otherwise
```

집계 결과는 항상 키를 동반한다. 값만 반환하면 이후 단계에서 어느 소유자·연도의 값인지 알 수 없고, 두 사람의 결과를 같은 변수에 담는 실수가 드러나지 않는다.

> **왜 키를 인자로 받는가.** 항목 배열만 받고 "호출부가 이미 필터링했다"고 가정하면 규칙은 주석으로만 존재한다. 키를 명시하면 위반이 함수 경계에서 드러난다. 키가 컴파일 시점에 알려진 경우(리터럴·상수)에는 타입 수준에서 거부되고, 런타임 UUID인 경우에는 같은 규칙을 실행 시점에 검사한다. 두 방어는 대체 관계가 아니라 보완 관계다.

### 7.2 귀속연도 결정

```
상환 완료: year( redemptions.redemption_date )
미상환   : year( 적용 차수의 evaluation_date )
```

미상환 상품의 적용 차수는 **다음 도래 평가일의 차수**로 가정한다. 세 경우에는 정할 수 없다 — 전 차수 경과 미상환(§9.2) · 평가일정 0건 · 만기 상환인데 일정 0건(이 경우는 상환일 기준이 먼저 적용된다). §7.5가 그 셋을 특수 처리 없이 흡수한다.

> **사용자 지정은 기본값을 바꾸지 않는다 (RD-02 해결, P4b).** 「다음 도래 평가일」을 **기본값으로 확정 유지**하고, 사용자 조정은 DOC-011 §4.7 `getForecast` 전용의 **비영속 `scenario` 인자**로 둔다(저장하지 않는다). 근거 셋 — ① 기본값을 바꾸면 DOC-011 §4.1·§4.3·§4.6·§4.8이 전부 값이 달라지는데 그것들은 이미 실측·검증된 상태다 ② §4.6의 `override`(비영속 시뮬레이션)라는 **동형의 선례**가 있다 ③ 영속 컬럼은 DOC-002 DQ-05(유도값 저장 금지)·RD-06과 정면으로 어긋나고 DOC-010 AQ-29의 우회면을 넓힌다. **스키마 변경 0, 호출부 변경 0.**
>
> **`scenario`의 규칙은 본 절이 아직 정하지 않았다** — 선택 가능한 차수 범위, 경과한 차수를 지정했을 때, 없는 차수를 지정했을 때가 열려 있다. 구현이 P4c로 배정되었으므로 그 규칙도 그때 여기에 신설한다(DOC-008 §9 SQ-07). 기본값만으로도 §7.5가 완결되는 것이 분리의 근거다.

### 7.3 연도별 금융소득

```
F(owner, year) = Σ taxableIncome(els)                     # 해당 소유자·연도의 ELS
               + tax_profiles.other_financial_income      # ELS 외 금융소득
```

합산 대상은 §7.1의 키에 속한 항목뿐이다. `other_financial_income`도 `(owner, year)`에 귀속된 값이므로 다른 소유자·연도의 값을 더할 수 없다.

`A`는 `tax_profiles.other_income_base`에서 조회한다. `A` 역시 개인 단위이므로 `F`와 같은 소유자의 값이어야 한다.

### 7.4 세후 회수액

```
netProceeds(owner, year) = Σ gross(els) − F(owner, year) × effectiveRate(F, A)
```

비과세 계좌 상품은 `taxableIncome = 0`이므로 세액이 부과되지 않고 `gross`가 전액 반영된다.

건강보험료는 차감하지 않는다. 세액만 반영하는 것이 이 정의이며, 보험료는 §6이 별 산식으로 낸다.

### 7.5 다년도 누적과 잔여 원금

§7.1~§7.4는 **유량**(그 해에 회수한 것)을 정의한다. 본 절은 **저량**(그 시점에 남아 있는 것)과 유량의 누계를 정의한다. 둘 다 집계 키가 `(소유자, 귀속연도)`이므로 §7에 둔다 — 이 장의 조직 원리는 「유량」이 아니라 그 키다(§7.1이 첫 절인 이유).

```
redeemedBy(els, Y) = true   if 상환완료 ∧ year(redemption_date) ≤ Y
                   = true   if 미상환 ∧ 적용차수 존재 ∧ year(적용차수 evaluation_date) ≤ Y
                   = false  otherwise

remainingPrincipal(owner, Y) = Σ P(els)  for els ∈ owned(owner) where ¬redeemedBy(els, Y)
cumulativeNet(owner, Y)      = Σ netProceeds(owner, y)  for y ∈ [Y₀, Y]
cumulativeAssets(owner, Y)   = cumulativeNet(owner, Y) + remainingPrincipal(owner, Y)
```

`Y₀`는 전망의 시작 연도이며 기준일의 연도다. 적용 차수는 §7.2가 정한다.

#### 측정 시점은 귀속연도의 말이다. 기준일이 아니다

DOC-005 §8.8의 「특정 **연도 말**」은 표기 문제가 아니라 **이중 계상 스위치**다. 기준일에서 재면 `asOf` 이후 같은 해에 상환되는 상품이 유량과 저량 **양쪽에** 잡힌다.

**검산** — A(원금 1억, 쿠폰 1천만, 2026-11-15 상환 가정) · B(원금 1억, 쿠폰 3천만, 2029 상환 가정) · `asOf = 2026-07-28` · 둘 다 일반계좌, 소유자 하나, ELS 외 금융소득 0, `A = 0`.

| 값 | 2026 | 2029 |
|---|---|---|
| `F` | 10,000,000 | 30,000,000 |
| 종합과세 | 아니다 (기준금액 이하) | **그렇다** |
| 총세액 | 1,540,000 | 4,620,000 |
| 추가납부 | **0** | **0** |
| 최종 부담률 | 15.40% | 15.40% |
| `netProceeds` | 110,000,000 − 1,540,000 = **108,460,000** | 130,000,000 − 4,620,000 = **125,380,000** |
| `remainingPrincipal` | B의 100,000,000 | 0 |
| `cumulativeAssets` | **208,460,000** | **233,840,000** |

**연도 말 기준이 아니면 2026년이 308,460,000이 된다** — `asOf`(7월 28일)에는 A가 아직 미상환이므로 잔여 원금에 A의 1억이 남고, 그 해 유량에도 A의 회수가 들어간다. **정확히 A의 원금만큼 과대**하다.

`cumulativeAssets(2029)`를 다른 경로로 검산하면 같은 값이 나온다: 투입 200,000,000 + 쿠폰 40,000,000 − 세금 6,160,000 = **233,840,000** (6,160,000 = 1,540,000 + 4,620,000).

> **2029년의 추가납부가 0인 것이 정상이다.** `F = 30,000,000`은 기준금액을 넘어 비교과세로 가지만 방식①(4,200,000)이 방식②(3,400,000)보다 크고, 그 결과 총세액이 원천징수액과 같아진다. §5.5가 규정한 「추가납부 0이 정상인 경우」이며 분기점은 77,600,000이다(§10.1 TC-01·TC-02가 그 사이 구간을 고정한다).

#### 이중 계상이 없는 이유 — 엄격 부등호가 배타적·전체적 분할을 만든다

`gross(els)`가 **원금 반환분을 포함하므로**(§4.1) 한 상품은 `redeemedBy`의 참·거짓에 따라 두 항 중 **정확히 하나**에만 들어간다.

| `redeemedBy(els, Y)` | `cumulativeNet(Y)` | `remainingPrincipal(Y)` |
|---|---|---|
| 참, 귀속연도 ∈ `[Y₀, Y]` | 들어간다 (그 해 한 번) | — |
| 참, 귀속연도 `< Y₀` | — | — |
| 거짓 | — | 들어간다 |
| 적용 차수 없음 | — | 들어간다 (전 연도) |

**둘째 줄이 `cumulativeAssets`라는 이름의 근거다.** `Y₀` 이전에 상환된 상품은 어느 항에도 없다 — 그 연도의 세율 시드가 없으면 세후로 계산할 수 없고 계약이 그 연도를 거부하기 때문이다(DOC-011 §4.6). 즉 이 값은 「보유 자산의 총액」이 아니라 **전망 구간 안에서의 누적**이며, 「총자산」으로 부르면 거짓이 된다(DOC-005 §8.8).

#### 적용 차수가 없는 상품은 배제하지 않는다

§7.2가 적용 차수를 정할 수 없는 경우가 셋이다 — E-07(전 차수 경과 미상환, §9.2) · 평가일정 0건 · 만기 상환인데 일정 0건. 셋 다 **특수 처리 없이 흡수된다**: `redeemedBy`를 귀속연도가 아니라 **상환 레코드의 존재**로 정의했기 때문이다. 앞의 둘은 `false`가 되어 전 연도의 `remainingPrincipal`에 남고 `netProceeds`에는 기여하지 않는다. 셋째는 첫 가지(상환 완료)가 처리한다.

**배제하면 그 원금이 화면에서 조용히 증발한다.** 그 상품의 원금이 여섯 행 전부의 잔여 원금에 남아 「회수 시점을 모르는 자산」으로 보이는 것이 **정확한 표현이며 E-07의 시각적 형태다** — 화면은 그 사실을 말해야 한다(사용자에게는 결함처럼 보이므로). 없는 판단을 지어내지 않는 것과 값을 숨기지 않는 것이 같은 규칙이다.

#### 추정 표식은 두 축이다

| 축 | 참인 조건 | 어디에 |
|---|---|---|
| `hasEstimates` | 그 행의 값이 적용 차수 가정에 의존한다 — 그 해에 상환이 **가정된** 미상환 상품이 있거나, 연도 말에 남은 미상환 상품이 있다 | 행 단위 표식 |
| 화면 전체 고지 | 미상환 상품이 하나라도 있다 | 표 위 (DOC-008 §5) |

**둘을 하나로 두면 행 표식의 정보량이 0이 된다** — 미상환 상품이 하나라도 있으면 잔여 원금이 그 가정에 의존하므로 모든 행이 참이 되기 때문이다. 그래도 행 축의 정의를 좁히지 않는 이유는, 좁히면 「이 행의 잔여 원금은 추정인데 표식이 없다」가 되어 화면이 확정이라 말하면서 시점을 모르는 원금을 보여주게 된다는 것이다.

---

## 8. 계산 파이프라인

화면 렌더링 시의 계산 순서. 하위 단계는 상위 단계의 결과에 의존한다.

```
1. 시세 조회        → asset_prices 최신값
2. 워스트오브 산출   → 상품별 W
3. 조건 판정        → 조기상환 / 리자드 / 이월, KI 상태   (보유중 상품만 — E-05)
4. 적용 차수 결정    → 사용자 지정 또는 다음 평가일        (경과 미상환 차수는 E-07)
5. 수령액 산출      → 확정값 또는 적용 쿠폰율 기반 추정값
6. 과세소득 산정     → 계좌유형·손실 규칙 적용
7. 연도별 집계      → (소유자, 연도) 키별 분할 — 키를 넘어 합산하지 않는다
8. 세액·보험료 계산  → 비교과세, 건강보험
9. 부담률·추가납부   → 표시값 산출
10. 다년도 누적·저량 → 연도 말 기준 잔여 원금, 누적 세후 회수·자산 (§7.5)
```

10단계는 SCR-402(다년도 전망)에서만 수행한다. 1~9를 전망 구간의 각 연도에 대해 반복한 뒤 그 결과를 누적하며, **측정 시점이 연도 말이라는 것**이 9단계까지와 다른 유일한 점이다(§7.5).

---

## 9. 예외 및 경계 처리

| ID | 상황 | 처리 |
|---|---|---|
| E-01 | 기초자산 현재가 누락 | `W = null`, 조건 판정 `null`. 화면에 "시세 없음" 표시 |
| E-02 | `F ≤ 0` | 모든 세액·보험료 `0` |
| E-03 | `A` 미입력 | `0`으로 정규화. 결과는 `A = 0`과 동일 |
| E-04 | `F ≤ comprehensiveTaxationThreshold` | 분리과세 종결. 방식①·② 미적용(`null`), 추가납부 `0`, 부담률 = `separateTaxationRate` |
| E-05 | 상환 완료 상품 | 조건 판정·시세 비교 수행하지 않음. **반환 계약은 §9.1** |
| E-06 | 노낙인 상품 (`ki_barrier IS NULL`) | KI 판정 생략, 리자드의 KI 요구 조건은 항상 충족으로 처리 |
| E-07 | 평가일이 과거이나 미상환 | 사용자에게 상환 처리 또는 이월 확인 요청. **반환 계약은 §9.2** |
| E-08 | 손실 상환 | 과세소득 `0`, 포트폴리오 손익은 음수 반영 |

E-05와 E-07은 "무엇을 하지 않는다" 또는 "사용자에게 요청한다"로만 기술되어 있어 반환값이 정의되지 않았다. 두 항목의 계약을 아래에서 확정한다.

### 9.1 E-05 반환 계약 — 상환 완료 상품

상태는 상환 레코드의 존재로 유도한다(DOC-002 §5.1). 조건 판정(§3.2·§3.3), KI 판정(§3.4), 시세 비교는 **보유중 상품에만 정의된다.**

```
asActive(params, redemption) = params     if redemption = NULL      # 보유중
                             = NULL       otherwise                 # 상환완료
```

판정 함수의 입력은 `asActive`의 반환값으로 한정한다. 상환 완료 상품에서는 `asActive`가 `NULL`을 반환하므로 판정 함수에 넘길 값이 애초에 존재하지 않는다.

| 대상 | 보유중 | 상환완료 |
|---|---|---|
| 조건 판정 (§3.2·§3.3) | `EARLY` / `LIZARD` / `CARRY_OVER`, 시세 없으면 `NULL` | **호출 불가** |
| KI 표시 등급 (§3.4) | `NO_KI` / `SAFE` / `WARNING` / `BELOW` / `TOUCHED`, 시세 없으면 `NULL` | **호출 불가** |
| KI 터치 후보 (§3.4 보조 판정) | `true` / `false` | **호출 불가** |

`asActive`가 `NULL`을 반환한 경우 호출부는 이를 **판정 생략**으로 표시한다. `CARRY_OVER`나 `SAFE` 같은 판정값으로 대체하지 않는다. 상환 완료 상품의 표시값은 판정 결과가 아니라 상환 실적(§4.2)이다.

> **`NULL`의 두 의미를 구분한다.** 판정 함수가 반환하는 `NULL`은 E-01(시세 없음 → 판정 보류)이고, `asActive`가 반환하는 `NULL`은 E-05(상환 완료 → 판정 대상 아님)다. 전자는 시세가 수집되면 값이 생기고, 후자는 영구적이다.

> **구현 방침.** "호출 불가"를 규율이 아니라 타입으로 강제한다. 판정 함수는 `asActive`만 부여할 수 있는 표식이 붙은 입력만 받으므로, 상환 완료 상품에 판정을 시도하는 코드는 컴파일되지 않는다.

### 9.2 E-07 반환 계약 — 평가일이 과거이나 미상환

```
overdueEvaluations(schedules, asOf, redemption)
  = []                                                        if redemption ≠ NULL
  = [ (schedule, daysOverdue) | evaluation_date < asOf ]       otherwise
                                                              (evaluation_date 오름차순)

daysOverdue = asOf − evaluation_date                          (양수)
```

- **상환 완료 상품은 빈 배열이다.** E-05가 우선한다. 상환 처리로 확인이 완결되었으므로 경과 차수를 다시 묻지 않는다.
- 평가일 당일은 경과로 보지 않는다. §7.2의 "다음 도래 평가일"과 경계를 공유한다.
- 경과한 차수를 **전부** 반환한다. 최근 1건만 반환하면 여러 차수가 연속으로 경과한 상품에서 앞 차수의 확인이 누락된다.
- 결과가 비어 있지 않은 상품은 "상환 처리 또는 이월 확인" 대상이다. **시스템이 이월을 자동 확정하지 않는다**(D-04와 같은 취지).
- 확인 이력을 저장하지 않으므로 조회 시마다 재산출된다(RD-06).

**`nextEvaluation`(§7.2)과의 관계.** 두 함수는 같은 평가일 목록을 서로 배타적인 두 구간으로 나눈다.

| 함수 | 대상 구간 | 반환 |
|---|---|---|
| `nextEvaluation` | `evaluation_date ≥ asOf` | 그중 최소 1건, 없으면 `NULL` |
| `overdueEvaluations` | `evaluation_date < asOf` | 전부, 없으면 `[]` |

`nextEvaluation`이 `NULL`이고 `overdueEvaluations`가 비어 있지 않으면 만기까지 전 차수가 경과했음에도 상환 실적이 없는 상태다. 적용 차수를 결정할 수 없으므로 귀속연도(§7.2)와 예상 수령액(§4.1)도 산출되지 않는다.

---

## 10. 검증 케이스

**회귀 테스트로 고정한다.** 계산 로직 변경 시 전 케이스가 통과해야 한다(K-08). 모든 케이스는 2026년 기준 상수를 사용한다.

### 10.1 비교과세 — 다른 종합소득 없음 (`A = 0`)

| ID | `F` | 추가납부 | 최종 부담률 | 검증 의도 |
|---|---|---|---|---|
| TC-01 | 20,000,000 | 0 | 15.40% | 기준금액 경계. 분리과세 종결 |
| TC-02 | 70,000,000 | 0 | 15.40% | 기준금액 초과이나 방식① 우위 |
| TC-03 | **77,600,000** | **0** | 15.40% | **분기점.** 방식①=방식② 정확히 일치 |
| TC-04 | 80,000,000 | 264,000 | 15.73% | 분기점 직후. 종합과세 전환 |
| TC-05 | 100,000,000 | **2,464,000** | **17.86%** | 대표 케이스 |
| TC-06 | 0 | 0 | 0% | 하한 경계 |

**TC-05 상세 검증**

```
방식① = 100,000,000 × 0.14 + T(0) = 14,000,000
방식② = 20,000,000 × 0.14 + T(80,000,000)
      = 2,800,000 + (80,000,000 × 0.24 − 5,760,000)
      = 2,800,000 + 13,440,000 = 16,240,000

귀속세액 = max(14,000,000, 16,240,000) − 0 = 16,240,000
지방세 포함 = 16,240,000 × 1.1 = 17,864,000
원천징수    = 100,000,000 × 0.154 = 15,400,000
추가납부    = 17,864,000 − 15,400,000 = 2,464,000
부담률      = 17,864,000 / 100,000,000 = 17.864%
```

### 10.2 `A` 정규화

| ID | 조건 | 기대 결과 |
|---|---|---|
| TC-07 | `F = 100,000,000`, `A = null` | 총세액 17,864,000 |
| TC-08 | `F = 100,000,000`, `A = 0` | 총세액 17,864,000 (TC-07과 **동일**) |

### 10.3 건강보험료

| ID | `F` | 가입유형 | 건강보험료 | 검증 의도 |
|---|---|---|---|---|
| TC-09 | 100,000,000 | 지역가입자 | 7,190,000 | 전액 반영 |
| TC-10 | 9,000,000 | 지역가입자 | 0 | 문턱 미달 |
| TC-11 | 100,000,000 | 직장가입자 | 5,752,000 | 초과분(8,000만)만 반영 |
| TC-12 | 15,000,000 | 직장가입자 | 0 | 문턱 미달 |
| TC-13 | 100,000,000 | 해당없음 | 0 | 미부과 |

### 10.4 실질 한계 부담률

| ID | 소득세율 | 기대 실질 한계율 |
|---|---|---|
| TC-14 | 6% | 14.73% |
| TC-15 | 24% | 34.53% |
| TC-16 | 45% | 57.63% |

### 10.5 손실 통산 금지

| ID | 시나리오 | 기대 결과 |
|---|---|---|
| TC-17 | A상품 −1,000만, B상품 +2,000만 (동일 소유자·연도) | 포트폴리오 손익 +1,000만, **과세 금융소득 2,000만** |

### 10.6 비과세 계좌

| ID | 시나리오 | 기대 결과 |
|---|---|---|
| TC-18 | `TAX_FREE` 계좌 상품, 이익 3,000만 | 과세 금융소득 0, 세액 0, 세후 수령 = 세전 수령 |

### 10.7 조건 판정

| ID | 시나리오 | 기대 결과 |
|---|---|---|
| TC-19 | `W = 0.92`, `B(1) = 0.90` | 조기상환 충족 |
| TC-20 | `W = 0.87`, `B(1) = 0.90`, `L(1) = 0.85` | 리자드 충족 |
| TC-21 | `W = 0.87`, `B = 0.90`, `L = 0.85`, KI 터치, `lizard_requires_no_ki = true` | 리자드 **미충족** → 이월 |
| TC-22 | 기초자산 3종 중 1종 시세 없음 | `W = null`, 판정 보류 |

---

## 11. 미결정 사항

| ID | 항목 | 상태 | 비고 |
|---|---|---|---|
| RD-01 | 세액·보험료 절사 단위 | 미결 | 증권사 원천징수액과 원 단위 오차 발생. 실제 고지 기준 확인 필요. **P1 구현 방침:** 절사 단위를 인자로 주입하고 기본값은 §2의 원 단위(`{ unit: 1, mode: TRUNCATE }`). 단위가 확정되면 호출부 인자만 교체하면 되므로 계산 모듈은 재작성하지 않는다. 10장 검증 케이스는 기대값이 모두 정수라 단위 선택과 무관하게 통과한다 |
| RD-02 | 미상환 상품의 기본 적용 차수 | **해결 (P4b)** | 기본값은 **「다음 도래 평가일의 차수」로 확정 유지**하고 사용자 지정은 **`getForecast` 전용 비영속 `scenario` 인자**로 둔다(저장하지 않는다). 근거는 §7.2의 각주에 있다 — 기본값을 바꾸면 이미 검증된 계약 넷의 값이 달라지고, §4.6의 `override`가 동형의 선례이며, 영속 컬럼은 DQ-05·RD-06과 어긋난다. **스키마 변경 0, 호출부 변경 0.** 산식은 §7.5가 신설했다. **`scenario`의 규칙(선택 범위·경과 차수·없는 차수)은 구현과 함께 P4c에 배정했다** — 기본값만으로 §7.5가 완결되므로 분리할 수 있고, 그 배정을 DOC-008 §9 SQ-07로 등재했다. 이 항목이 「해결」인 것은 **기본값과 조정의 형태가 정해졌다**는 뜻이며 조정 UI가 섰다는 뜻이 아니다 |
| RD-03 | 평가일 영업일 조정 규칙 | **미결 (P6 컷 1에서 기산 규약만 닫혔다)** | **★ P6 컷 1 — 기산 규약을 `−1일`로 정했고 잔여 둘이 남는다.** 실보유 ELS 11건의 증권사 통지서를 대조했더니 평가일이 산식보다 **일정하게 하루 앞**이었다 — **65차수 전부에서 −1일**이다. **이것은 영업일 조정이 아니다**: 휴장일 조정이면 계산된 날이 휴일에 걸린 **일부 차수만** 움직이고 방향도 통상 **다음 영업일(+n)**이다. 전 차수가 균일하게 −1이면 **기산 규약의 차이**(「n개월 후의 전일」)이며, 그래서 산식으로 닫을 수 있었다. 생성 산식은 `발행일 + n × 평가주기 − 1일`이 되고 상수는 `lib/domain`에 있으며 호출부가 **필수 인자로 명시한다**(기본값을 주면 새 호출부가 조용히 규약을 안 따른다). 월말에서 순서가 갈리므로 **월 이동 → 클램프 → 일 이동**을 정본으로 못박았다(DOC-002 §4.8에 대조표가 있다). **부수로 이 항목이 지목한 충돌이 해소됐다** — 「조정 경로를 만들면 DOC-011 §5.2의 전체 교체와 충돌한다」가 그것인데, **규약이 전역이므로 재생성이 멱등이다**(저장값이 이미 새 산식의 결과다). 그래서 **차수별 입력 칸을 만들지 않았고 §5.2에 예외도 두지 않았다.** **★ 잔여 둘 — 이 항목은 닫히지 않는다:** ⓐ **휴장일 자료에 의한 자동 영업일 조정**은 여전히 수단이 없다. 선행은 Q-03(시세 공급자)이 아니라 **휴장일 자료원**이며, 테이블 하나 + 시드 + 연 1회 갱신 운영을 끌고 온다. ⓑ **★ offset이 전역 규약이므로 상품별 규약 차이를 표현하지 못한다.** 현 포트폴리오 11건이 한 규약이라 지금은 맞지만, 다른 규약의 상품이 오면 **그 상품의 전 차수가 하루씩 틀리고 화면에 그 사실을 말할 자리가 없다.** 되살릴 조건이 그것이며 그때 필요한 것은 차수별 입력 칸 또는 상품별 필드이고, **둘 다 §5.2 전체 교체와의 충돌을 다시 결정하게 만든다**(위에서 해소된 것이 그 조건에서 되돌아온다). **§10의 TC-01~22과 §3의 판정 규칙은 무변경이다** — 절대 규칙 #1에 따라 정본을 건드리지 않았다. 아래는 P4.5 정정의 기록이다. 휴장일 처리. ~~현재는 사용자 수정에 의존~~ → **의존 대상이 실재하지 않는다 (P4.5 정정).** 이 비고는 「조정이 필요하면 사용자가 손으로 고친다」를 폴백으로 전제했는데 **평가일에는 입력 칸이 없다** — SCR-204는 `발행일 + n × 평가주기`로 생성하며 저장마다 재생성한다(DOC-008 §5 SCR-204·DOC-002 §4.8). 즉 이 미결에는 폴백이 없고, **조정이 필요한 상품이 오면 지금은 방법이 없다.** 저장 자체는 옳다(조정된 날짜를 담을 자리가 이미 있다). 닫을 때 함께 정할 것 둘: ① 조정 경로를 화면에 두는가 ② 둔다면 DOC-011 §5.2의 **전체 교체**와 충돌하므로(수정 저장이 조정한 날짜를 되돌린다) 그 예외를 어떻게 두는가. **계산 규칙의 변경이 아니다** — 본 문서 §10의 TC-01~22는 이 항목과 무관하고 그대로다 |
| RD-04 | 지역가입자 세대 합산 지원 | 미결 | v1 제외(A-06). 지원 시 세대 엔티티 필요 |
| RD-05 | 다년도 걸친 이월 상품의 부담률 재계산 시점 | 미결 | 연도 경계에서의 추정치 갱신 정책 |
| RD-06 | 이월 확인 이력의 저장 여부 | 미결 | §9.2가 경과 차수를 조회마다 재산출하므로, 사용자가 이월을 확인한 뒤에도 같은 차수가 계속 표시된다. 이력을 저장하면 반복 표시가 사라지나 테이블이 하나 늘고 "확인"의 의미(단순 열람인지 이월 확정인지)를 정의해야 한다. 상품 수가 적은 초기에는 재산출로 충분하다 |

---

*본 문서는 프로젝트 진행에 따라 갱신되며, 주요 변경은 변경 이력에 기록한다.*
